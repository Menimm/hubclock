# Base URL the frontend uses to reach the backend API.
# Defaults to the local backend origin. Change it if you expose the API through a proxy.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Development server port for `npm run dev`.
VITE_DEV_PORT=5173
