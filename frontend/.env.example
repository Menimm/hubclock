# Base URL the frontend uses to reach the backend API.
# Keep "/api" if you proxy requests from the web server to the backend.
# Otherwise, point to the backend origin (e.g. http://127.0.0.1:8000).
VITE_API_BASE_URL=/api
